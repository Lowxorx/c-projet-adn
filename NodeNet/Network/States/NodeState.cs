namespace NodeNet.Network.States
{
    public enum NodeState
    {
        JOB_START,
        WORK,
        WAIT,
        FINISH,
        ERROR,
        NODE_IS_WORKING,
        IN_PROGRESS,
        DEAD
    }
}
