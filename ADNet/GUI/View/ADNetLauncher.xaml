<Window
    x:Class="ADNet.GUI.View.ADNetLauncher"
    xmlns:intr="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:vm="clr-namespace:ADNet.GUI.ViewModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ADNetLauncher"
    Width="300"
    Height="300"
    mc:Ignorable="d" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Center">
    <Window.DataContext>
        <vm:VMADNetLauncher/>
    </Window.DataContext>
    <intr:Interaction.Triggers>
        <intr:EventTrigger EventName="Loaded">
            <intr:InvokeCommandAction Command="{Binding WindowLoaded}" />
        </intr:EventTrigger>
    </intr:Interaction.Triggers>
    <Grid >
        <Button Content="Valider"
                Command="{Binding IcommandBtnClick}" HorizontalContentAlignment="Center" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,30" Width="82"/>
        <Label Content="ADNet" FontWeight="Bold" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Top" FontSize="16"/>
        <RadioButton  Command="{Binding IcommandRdbCliClick}" Content="Mode client" x:Name="RdbClient" HorizontalAlignment="Left" Margin="100,77,0,0" VerticalAlignment="Top" GroupName="mode" FontSize="14"/>
        <RadioButton Command="{Binding IcommandRdbServClick}" Content="Mode serveur" GroupName="mode" FontSize="14" HorizontalAlignment="Left" VerticalContentAlignment="Center" VerticalAlignment="Center" Margin="100,40,0,0" HorizontalContentAlignment="Center"/>
        <TextBox TextWrapping="Wrap" Text="TextBox" IsEnabled="{Binding TxtIpEnabledProp}" FontSize="14" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Center" Height="21" Width="140" Margin="0,0,0,31"/>
    </Grid>

</Window>
